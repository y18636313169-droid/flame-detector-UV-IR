# STM32L1 boards in this project use SWD.  Keep the initial clock conservative:
# some boards fail during OpenOCD init at the adapter's automatic/default speed.
source [find interface/stlink.cfg]
transport select swd
adapter speed 950
